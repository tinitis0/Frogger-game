#pragma once
#include <SFML/Graphics.hpp>
#include "MainObjects.h"


class LogBeams : public MainObjects
{
public:
	LogBeams(float posX, float posY);
	void Move(sf::Vector2u size);
private:

	float originalPosX, originalPosY;
};

class LogBeams2 : public MainObjects
{
public:
	LogBeams2(float posX, float posY);
	void Move(sf::Vector2u size);
private:

	float originalPosX, originalPosY;
};

class LogBeams3 : public MainObjects
{
public:
	LogBeams3(float posX, float posY);
	void Move(sf::Vector2u size);
private:

	float originalPosX, originalPosY;
};

class LogBeams4 : public MainObjects
{
public:
	LogBeams4(float posX, float posY);
	void Move(sf::Vector2u size);
private:

	float originalPosX, originalPosY;
};

class LogBeams5 : public MainObjects
{
public:
	LogBeams5(float posX, float posY);
	void Move(sf::Vector2u size);
private:

	float originalPosX, originalPosY;
};

class LogBeams6 : public MainObjects
{
public:
	LogBeams6(float posX, float posY);
	void Move(sf::Vector2u size);
private:

	float originalPosX, originalPosY;
};
