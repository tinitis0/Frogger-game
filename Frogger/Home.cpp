#include "Home.h"
#include "Definitions.h"

Home::Home(float posX, float posY)
{
	shape.setSize(sf::Vector2f(80.f, 80.0f));
	shape.setPosition(sf::Vector2f(posX, posY));

	originalPosX = posX;
	originalPosY = posY;

	shape.setFillColor(sf::Color HOME_COLOR);
}

Home2::Home2(float posX, float posY)
{
	shape.setSize(sf::Vector2f(80.f, 80.0f));
	shape.setPosition(sf::Vector2f(posX, posY));

	originalPosX = posX;
	originalPosY = posY;

	shape.setFillColor(sf::Color HOME_COLOR);
}

Home3::Home3(float posX, float posY)
{
	shape.setSize(sf::Vector2f(80.f, 80.0f));
	shape.setPosition(sf::Vector2f(posX, posY));

	originalPosX = posX;
	originalPosY = posY;

	shape.setFillColor(sf::Color HOME_COLOR);
}

Home4::Home4(float posX, float posY)
{
	shape.setSize(sf::Vector2f(80.f, 80.0f));
	shape.setPosition(sf::Vector2f(posX, posY));

	originalPosX = posX;
	originalPosY = posY;

	shape.setFillColor(sf::Color HOME_COLOR);
}